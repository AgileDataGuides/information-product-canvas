// For demo/static builds: disable SSR and prerendering (pure SPA mode)
// For normal builds: these are ignored since adapter-auto handles it
export const ssr = false;
export const prerender = false;
